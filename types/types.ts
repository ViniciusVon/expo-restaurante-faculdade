export type ItemCardapio = {
    id: string;
    nome: string;
    descricao: string;
    preco: number;
    imagem: any;
};

export type Cliente = {
    nome: string;
    endereco: string;
    email: string;
};
